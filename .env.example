# ── Provider selection ────────────────────────────────────────────────────────
# PRIMARY_PROVIDER controls which AI service is used first for transcription
# and cleanup. The other provider (if its key is set) acts as automatic fallback
# when the primary fails or is unreachable.
# Allowed values: groq (default), openai
PRIMARY_PROVIDER=groq

# ── API Keys ──────────────────────────────────────────────────────────────────
# Required when PRIMARY_PROVIDER=groq (also used as fallback when primary is openai)
GROQ_API_KEY=your_groq_key_here

# Required when PRIMARY_PROVIDER=openai (also used as fallback when primary is groq)
# OPENAI_API_KEY=your_openai_key_here

# ── Groq model settings ───────────────────────────────────────────────────────
TRANSCRIPTION_MODEL=whisper-large-v3-turbo
CLEANUP_MODEL=openai/gpt-oss-20b

# ── OpenAI model settings (used when OpenAI is primary or fallback) ───────────
OPENAI_TRANSCRIPTION_MODEL=whisper-1
OPENAI_CLEANUP_MODEL=gpt-4o-mini

# ── Other settings ────────────────────────────────────────────────────────────
LANGUAGES=Portuguese,English
HOTKEY=alt+\
SILENCE_THRESHOLD=1
SILENCE_DURATION=2.0
MIN_CHUNK_DURATION=3.0

CLEANUP_SYSTEM_PROMPT="You are a transcription cleanup assistant. Clean up the transcription by fixing punctuation, grammar, and capitalization, and removing filler words (uh, um, like, etc.). Do not rephrase, summarize, or add anything. Return only the cleaned text with no explanation.\n\nDetect the language of the input and respond in that same language."
